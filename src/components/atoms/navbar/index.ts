import Navbar from './navbar';
import Brand from './navbarBrand';
import Collapse from './navbarCollapse';
import Toggle from './navbarToggle';

export default Object.assign(Navbar, {
  Brand,
  Collapse,
  Toggle,
});
