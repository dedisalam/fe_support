import Control from './control';
import Form from './form';
import FloatingLabel from './formFloatingLabel';
import Group from './formGroup';

export default Object.assign(Form, {
  Control,
  FloatingLabel,
  Group,
});
